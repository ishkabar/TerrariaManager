<UserControl x:Class="Ogur.Terraria.Manager.Devexpress.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:converters="clr-namespace:Ogur.Terraria.Manager.Devexpress.Converters"
             Background="#1e1e1e">

    <UserControl.Resources>
        <!-- Converter -->
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBooleanConverter"/>

        <!-- Tab Item Style -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}"
                                Margin="0,0,2,0">
                            <ContentPresenter ContentSource="Header"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2d2d30"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3e3e42"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status Bar - wspólny dla wszystkich tabów -->
        <Border Grid.Row="0"
                Background="#2d2d30"
                CornerRadius="5"
                Padding="15,10"
                Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="🖥️ Server Status:"
                           Foreground="White"
                           FontSize="14"
                           VerticalAlignment="Center" />

                <TextBlock Grid.Column="1"
                           Text="{Binding StatusText}"
                           Foreground="{Binding StatusText, Converter={StaticResource StatusToColorConverter}}"
                           FontWeight="Bold"
                           FontSize="14"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0" />

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal">
                    <dx:SimpleButton Content="Connect"
                                     Command="{Binding ConnectCommand}"
                                     IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBooleanConverter}}"
                                     Style="{StaticResource PrimaryButton}"
                                     Height="32"
                                     MinWidth="100"
                                     Margin="0,0,8,0" />

                    <dx:SimpleButton Content="Disconnect"
                                     Command="{Binding DisconnectCommand}"
                                     IsEnabled="{Binding IsConnected}"
                                     Style="{StaticResource SecondaryButton}"
                                     Height="32"
                                     MinWidth="100" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- TabControl -->
        <TabControl Grid.Row="1"
                    Background="#1e1e1e"
                    BorderThickness="0"
                    x:Name="MainTabControl"
                    SelectionChanged="MainTabControl_SelectionChanged">
            <TabItem Header="🖥️ Console" x:Name="ConsoleTab" />
            <TabItem Header="🎮 Commands" x:Name="CommandsTab" />
            <TabItem Header="⚙️ Settings" x:Name="SettingsTab" />
        </TabControl>
    </Grid>
</UserControl>